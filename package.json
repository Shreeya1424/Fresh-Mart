{
  "name": "freshmart",
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run dev --prefix smms-frontend",
    "start": "npm run start --prefix smms-frontend",
    "build": "npm run build --prefix smms-frontend",
    "install-all": "npm install --prefix smms-frontend"
  }
}
